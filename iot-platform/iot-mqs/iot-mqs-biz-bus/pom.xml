<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>iot-mqs</artifactId>
        <groupId>org.springblade</groupId>
        <version>4.9.0.RELEASE</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <modelVersion>4.0.0</modelVersion>
    <artifactId>iot-mqs-biz-bus</artifactId>
    <name>${project.artifactId}</name>
    <description>消息治理服务-业务模块-协议总线 (统一事件 / 三相管道 / SPI 钩子)</description>

    <dependencies>
        <!-- 业务实体 + 服务依赖 (stage 内调既有 service 落库 / 解物模型) -->
        <dependency>
            <groupId>org.springblade</groupId>
            <artifactId>iot-mqs-entity</artifactId>
            <version>4.9.0.RELEASE</version>
        </dependency>
        <dependency>
            <groupId>org.springblade</groupId>
            <artifactId>iot-mqs-biz</artifactId>
            <version>4.9.0.RELEASE</version>
        </dependency>
        <!-- 桥接入站 RULE_TRIGGER 分流触发规则策略 -->
        <dependency>
            <groupId>org.springblade</groupId>
            <artifactId>iot-rule-api</artifactId>
            <version>4.9.0.RELEASE</version>
        </dependency>

        <!-- 基础工具 (Hutool / 上下文 / 缓存 / RocketMQ / Kafka / 动态线程池) -->



        <!-- Micrometer 监控指标 -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-core</artifactId>
        </dependency>

        <!-- Spring Boot 自动配置 + 注解处理 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- 测试 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.4</version>
                <configuration>
                    <skip>false</skip>
                    <skipTests>false</skipTests>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
