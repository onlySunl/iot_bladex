<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mqttsnet.thinglinks.ota.mapper.OtaUpgradeRecordsMapper">
    <!--
        代码生成器 by 2024-01-12 22:42:04
        自定义sql建议在base文件夹同级新建ext文件夹，并新建同名且同namespace的xml进行编写。方便修改字段时，重新生成此文件。
    -->

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.mqttsnet.thinglinks.ota.entity.OtaUpgradeRecords">
        <id column="id" property="id"/>
        <result column="task_id" property="taskId"/>
        <result column="device_identification" property="deviceIdentification"/>
        <result column="upgrade_status" property="upgradeStatus"/>
        <result column="progress" property="progress"/>
        <result column="error_code" property="errorCode"/>
        <result column="error_message" property="errorMessage"/>
        <result column="start_time" property="startTime"/>
        <result column="end_time" property="endTime"/>
        <result column="success_details" property="successDetails"/>
        <result column="failure_details" property="failureDetails"/>
        <result column="log_details" property="logDetails"/>
        <result column="remark" property="remark"/>
        <result column="deleted" property="deleted"/>
        <result column="created_time" property="createdTime"/>
        <result column="created_org_id" property="createdOrgId"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, task_id, device_identification, upgrade_status, progress, error_code, error_message,
        start_time, end_time, success_details, failure_details, log_details, remark, deleted,
        created_time, created_org_id
    </sql>

    <!-- 逻辑删除条件 - 可在其他SQL中引用 -->
    <sql id="LogicDelete_Condition">
        <if test="true">deleted = 0</if>
    </sql>

    <select id="selectOtaUpgradeRecordsSummary"
            resultType="com.mqttsnet.thinglinks.ota.dto.OtaUpgradeRecordsSummaryResultDTO">
        SELECT
        task_id AS taskId,
        COUNT(*) AS totalCount,
        COUNT(CASE WHEN upgrade_status = 0 THEN 1 END) AS pendingUpgradeCount,
        COUNT(CASE WHEN upgrade_status = 1 THEN 1 END) AS upgradingCount,
        COUNT(CASE WHEN upgrade_status = 2 THEN 1 END) AS upgradeSuccessCount,
        COUNT(CASE WHEN upgrade_status = 3 THEN 1 END) AS upgradeFailureCount
        FROM ota_upgrade_records
        WHERE
        <include refid="LogicDelete_Condition"/>
        <if test="ew != null and ew.sqlSegment != null and ew.sqlSegment != ''">
            AND ${ew.sqlSegment}
        </if>
    </select>


</mapper>
